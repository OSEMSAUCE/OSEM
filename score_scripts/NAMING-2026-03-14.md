Fields Needing Suffix Changes
Qty suffix (numeric quantities/tallies):
✅plantedCount → plantedCountQty ✅ (you already did this one!)
✅allocated → allocatedQty
✅claimCount → claimQty (it's a tree quantity, not count of claims)
✅employmentClaim → employmentClaimQty
✅treeCapAnnualQty → treeCapAnnualQtyQty
## Pct suffix (percentages):
qualityPct → qualityPct
survivalRatePct → survivalRatePctPct
## Desc suffix (long-form text):
organizationDesc → organizationDesc
projectDesc → projectDesc
landDesc → landDesc
cropDesc → cropDesc
polygonNotes → polygonDesc
## Amt suffix (monetary values):
pricePerUnit → KEEP AS-IS (per our earlier discussion - "price" is clear enough)
pricePerUnitUSD → KEEP AS-IS (same reasoning)
## Name suffix (human-readable names):
editedBy → editedByName (appears in many tables)
## Standard terms (remove prefix):
latitude → latitude
longitude → longitude