# OSEM - Open Source Environment Map Demo
# Copy this to .env and fill in your values
# OSEM has NO database credentials - it only calls ReTreever's API

# Mapbox (REQUIRED for map display)
# Get a free token at: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=your-mapbox-token-here

# ReTreever API (REQUIRED - OSEM fetches ALL data from here)
# Point to your ReTreever instance (SubWoof uses this variable)
PUBLIC_API_URL=http://localhost:5173
