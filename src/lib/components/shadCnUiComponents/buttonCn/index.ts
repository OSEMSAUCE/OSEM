import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./buttonCn.svelte";

export {
	Root,
	type ButtonProps as Props,
	//
	Root as ButtonCn,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
};
